# Gradle
.gradle/
**/build/
.codex*

# Kotlin
.kotlin/

# Android Studio / IntelliJ
.idea/
*.iml

# Local environment
local.properties

# Android generated files
captures/
.externalNativeBuild/
.cxx/

# Release outputs and signing secrets
*.apk
*.aab
*.ap_
*.jks
*.keystore
keystore.properties
signing.properties
app/release/

# OS files
.DS_Store
Thumbs.db

kodik_*
pages/

AGENTS.md

.tmp
logs

# Local-only scripted extension fixtures used by parsers tests, not shipped in the main repo
parsers/src/test/resources/beakokit/extensions/
parsers/src/test/resources/beakokit/resolvers/
# Raw captured (obfuscated) API responses used only to test decoding a specific site's response
# format - real scraped third-party data, not a hand-written fixture, so it doesn't belong in the
# repo either.
parsers/src/test/resources/beakokit/miruro/
