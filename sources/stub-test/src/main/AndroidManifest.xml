<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="Hibiki Source: Stub Test"
        android:icon="@drawable/hibiki_app_icon">

        <!-- Discovery anchor only. Exists so the host can find this package via
             PackageManager.queryIntentActivities; finishes immediately if ever launched. -->
        <activity
            android:name=".StubSourceDiscoveryActivity"
            android:exported="true">
            <meta-data
                android:name="hibiki.source.class"
                android:value="org.akkirrai.hibiki.source.stub.StubSource" />
            <meta-data
                android:name="hibiki.source.contractVersion"
                android:value="1" />
            <intent-filter>
                <action android:name="org.akkirrai.hibiki.action.SOURCE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

    </application>

</manifest>
